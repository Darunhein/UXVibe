-- Migration: create RESPUESTAS table (adjust to your RDBMS dialect)
-- Postgres
-- CREATE TABLE respuestas (
--   id_respuesta BIGSERIAL PRIMARY KEY,
--   email_usuario VARCHAR(255),
--   nombre_prueba VARCHAR(255),
--   nombre_participante VARCHAR(255),
--   pregunta VARCHAR(255),
--   respuesta TEXT,
--   numerico INTEGER
-- );

-- MySQL
-- CREATE TABLE respuestas (
--   id_respuesta BIGINT AUTO_INCREMENT PRIMARY KEY,
--   email_usuario VARCHAR(255),
--   nombre_prueba VARCHAR(255),
--   nombre_participante VARCHAR(255),
--   pregunta VARCHAR(255),
--   respuesta TEXT,
--   numerico INT
-- );

-- Oracle (example)
-- CREATE TABLE RESPUESTAS (
--   ID_RESPUESTA NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
--   EMAIL_USUARIO VARCHAR2(255),
--   NOMBRE_PRUEBA VARCHAR2(255),
--   NOMBRE_PARTICIPANTE VARCHAR2(255),
--   PREGUNTA VARCHAR2(255),
--   RESPUESTA CLOB,
--   NUMERICO NUMBER
-- );

-- SQLite
-- CREATE TABLE respuestas (
--   id_respuesta INTEGER PRIMARY KEY AUTOINCREMENT,
--   email_usuario TEXT,
--   nombre_prueba TEXT,
--   nombre_participante TEXT,
--   pregunta TEXT,
--   respuesta TEXT,
--   numerico INTEGER
-- );

-- After creating the table, ensure the column names match the queries in ParticipantDao.java
-- (EMAIL_USUARIO, NOMBRE_PRUEBA, NOMBRE_PARTICIPANTE, PREGUNTA, RESPUESTA, NUMERICO, FECHA)